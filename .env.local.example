# Supabase Configuration
# Get these values from your Supabase project dashboard (Settings > API)

# Your Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase service role key (for server-side operations)
# DO NOT use the anon key for server-side operations as it has limited permissions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Supabase anon key (for client-side operations, if needed later)
# SUPABASE_ANON_KEY=your-anon-key-here